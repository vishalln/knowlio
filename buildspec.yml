version: 0.2
phases:
  install:
    commands:
      - echo Installing dependencies...
      - pip install -r requirements.txt
  build:
    commands:
      - echo Synthesizing CDK stack...
      - cdk synth
artifacts:
  files:
    - "**/*"